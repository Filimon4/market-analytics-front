services:
  frontend:
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL}
        VITE_API_BASE_URL_API: ${VITE_API_BASE_URL_API}
    ports:
      - "80:80"
    restart: unless-stopped
